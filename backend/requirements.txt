# 2026-08-18 (güvenlik denetimi madde 19): tüm sürümler sabitlendi.
# Öncesinde hiçbir paketin sürümü belirtilmemişti -- `pip install` her
# çalıştırıldığında farklı (ve potansiyel olarak uyumsuz ya da güvenlik
# açığı içeren) sürümler kurulabiliyordu, reproducibility de yoktu.
# Aşağıdaki sürümler, bu makinede o an ÇALIŞAN/test edilmiş sürümlerdir
# (`pip freeze` ile alındı) -- davranış değişikliği YOK, sadece mevcut
# çalışan hal "dondurulmuş" oldu. Düzenli olarak `pip list --outdated`
# veya `pip-audit`/`safety check` ile güncel/güvenli tutulmalı.
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
apscheduler==3.11.3
playwright==1.62.0
# 2026-09-05 (güvenlik taramasi, pip-audit): 6.15.0'da 3 CVE vardi
# (CVE-2026-84309/84310/84311) -- kotu hazirlanmis bir PDF, TreeObject
# dongusu / ic ice outline / XForm genislemesi uzerinden sonsuz
# donguye ya da CPU/bellek tuketimine yol aciyordu. WEB_CONCURRENCY=1
# oldugu icin (tek surec) boyle bir PDF tum sunucuyu kilitleyebilirdi.
# pypdf sadece bot.py/aktar.py'de cetatenie.just.ro'dan indirilen
# PDF'leri okumak icin kullaniliyor (kullanici yuklemesi yok) ama
# tedarik zinciri riskini kapatmak icin 6.16.1'e sabitlendi.
pypdf==6.16.1
beautifulsoup4==4.15.0
requests==2.34.2
# gunicorn: bu makinede (Windows) kurulu değil -- Unix'e özgü bir paket,
# sadece Render (Linux) üretim sunucusunda gerekiyor. Sürüm, yerelde test
# edilemediği için PyPI'daki güncel kararlı sürümle sabitlendi.
gunicorn==26.0.0
python-dotenv==1.2.2
slowapi==0.1.10

# 2026-08-17: kritik saf fonksiyonlar için birim testler eklendi (bkz.
# tests/test_dosya_utils.py) -- bu oturumda birkaç kez elle test ederek
# bulduğumuz gerçek bug'lar (ör. "603" yanlış eşleşmesi, "indice" yazım
# farkı, REZULTATE/INVITATII ART.8.1 yanlış reddi), otomatik testlerle çok
# daha erken yakalanabilirdi.
pytest==9.1.1

# PDF indirmelerinde gerçek bir tarayıcı TLS parmak izini taklit eder --
# cetatenie.just.ro'nun bot-engelleme sistemini aşmaya yardımcı olur
# (bkz. bot.py "2026-08-14 tespiti" notu). Kurulu değilse uygulama yine
# çalışır, sadece bu korumadan yararlanamaz.
curl_cffi==0.16.0

# 2026-08-19: Hata izleme (Sentry). SENTRY_DSN ortam değişkeni ayarlı
# değilse main.py sentry_sdk.init()'i hiç çağırmıyor -- yani bu paket
# kuruluysa da DSN yoksa devre dışı kalır, hiçbir yan etkisi olmaz.
sentry-sdk[fastapi]==2.68.0

# 2026-08-22: Bulut yedekleme (Backblaze B2). B2'nin S3-uyumlu API'sine
# bağlanmak için kullanılıyor -- B2_* ortam değişkenleri ayarlı değilse
# main.py b2_yedegini_yukle()'yi hiç çağırmıyor, bu paket kuruluysa da
# devre dışı kalır (SENTRY_DSN ile aynı desen).
boto3==1.43.78

# 2026-08-23: Admin paneli girişi artık HTML form (POST, kullanici_adi/sifre
# alanlari) kullanıyor -- eskiden HTTP Basic Auth'du, form-data ayrıştırmak
# için FastAPI'nin Form(...) desteği bu pakete ihtiyaç duyuyor.
python-multipart==0.0.32

# 2026-08-30 (Gece Nöbeti -- Faz 1): admin panelinin (PWA) tarayıcı-native
# Web Push bildirimlerini göndermek için. BİLİNÇLİ OLARAK 1.14.1 (2.x DEĞİL)
# -- pywebpush>=2.0 ve onun çektiği py-vapid>=1.9.4, cryptography>=47
# zorunlu kılıyor; bu makinede (ilgisiz başka bir proje yüzünden kurulu)
# pyOpenSSL 24.2.1 ise cryptography<44 istiyor, ikisi ÇAKIŞIYOR ve
# `import main` bu yüzden eventlet/pyOpenSSL üzerinden patlıyordu. 1.14.1
# gevşek bir alt sınır istiyor (cryptography>=2.6.1), hem 2.x'in
# gereksiz aiohttp bağımlılığını da getirmiyor. py-vapid AYRICA pinlendi
# çünkü pywebpush'un kendi alt sınırı (>=1.7.0) tek başına bırakılırsa pip
# yine en yeni (1.9.4, cryptography>=46) py-vapid'i seçip aynı çakışmayı
# geri getirebiliyor.
pywebpush==1.14.1
py-vapid==1.9.0
